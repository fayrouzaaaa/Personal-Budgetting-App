/**
 * The entry point of the Personal Budgeting Application.
 * * <p>The Main class contains the {@code main} method, which serves as the
 * starting point for the program's execution. It initializes the user interface
 * by launching the initial {@link SignupScreen}, allowing the user to
 * register or navigate to the login page.</p>

 */
public class Main {

    /**
     * The main method that launches the application.
     * * @param args Command-line arguments (not utilized in this application).
     */
    public static void main(String[] args) {
        // Initialize and display the first screen of the application
        SignupScreen s = new SignupScreen();


    }
}